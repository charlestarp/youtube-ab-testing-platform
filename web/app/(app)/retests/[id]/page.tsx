// Retests reuse the same detail view as tests, but under /retests/<id> so the
// sidebar highlights Retests and back navigation returns to the Retests list.
export { default } from "../../tests/[id]/page";
